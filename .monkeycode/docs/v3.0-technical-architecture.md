# BizLens v3.0 技术架构设计

> 详细技术规格和实现方案

---

## 📁 项目结构

```
bizlens/
├── frontend/                    # Next.js 15 前端
│   ├── app/
│   │   ├── (main)/             # 主布局
│   │   │   ├── boards/         # 看板管理
│   │   │   │   ├── page.tsx
│   │   │   │   ├── viewer.tsx
│   │   │   │   └── editor.tsx
│   │   │   ├── chat/           # AI 对话
│   │   │   │   └── page.tsx
│   │   │   ├── dashboards/     # 数据大屏
│   │   │   │   └── page.tsx
│   │   │   └── data-sources/   # 数据源管理
│   │   │       └── page.tsx
│   │   └── api/                # API Routes
│   ├── components/
│   │   ├── boards/             # Boards 组件
│   │   └── shared/             # 共享组件
│   ├── lib/
│   │   ├── api-client.ts       # API 客户端
│   │   └── utils.ts
│   └── types/
│       └── index.ts
├── backend/                     # Go 后端
│   ├── cmd/
│   │   └── main.go
│   ├── internal/
│   │   ├── handler/            # HTTP Handler
│   │   │   ├── boards.go
│   │   │   └── context.go
│   │   ├── service/            # 业务逻辑
│   │   │   ├── semantic/
│   │   │   │   ├── layer.go       # 语义层
│   │   │   │   ├── metrics.go     # 指标管理
│   │   │   │   ├── dimensions.go  # 维度管理
│   │   │   │   └── sql-gen.go     # SQL 生成
│   │   │   ├── agent/
│   │   │   │   ├── orchestrator.go # Agent 编排
│   │   │   │   ├── query.go        # Query Agent
│   │   │   │   ├── insight.go      # Insight Agent
│   │   │   │   └── quality.go      # Quality Agent
│   │   │   └── compute/
│   │   │       ├── duckdb.go       # DuckDB 引擎
│   │   │       ├── cache.go        # 缓存引擎
│   │   │       └── reactive.go     # 响应式计算
│   │   ├── model/            # 数据模型
│   │   │   ├── metric.go
│   │   │   ├── board.go
│   │   │   └── user.go
│   │   ├── repository/       # 数据访问
│   │   │   ├── postgres.go
│   │   │   ├── git.go
│   │   │   └── storage.go
│   │   └── config/
│   └── go.mod
├── context/                     # Context 模板
│   ├── metrics/
│   ├── dimensions/
│   └── rules/
└── scripts/
    └── init-db.sql
```

---

## 🗄️ 数据库设计

### 1. 核心表结构

```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(100) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    role VARCHAR(50) DEFAULT 'user', -- admin, data-team, business
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 指标定义表
CREATE TABLE metrics (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(100) NOT NULL,
    description TEXT,
    formula TEXT NOT NULL, -- SQL 或表达式
    tables TEXT[], -- 涉及的表
    dimensions TEXT[], -- 相关维度
    tags TEXT[],
    owner VARCHAR(100),
    format VARCHAR(50), -- number, percentage, currency
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(name)
);

-- 维度定义表
CREATE TABLE dimensions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(100) NOT NULL,
    description TEXT,
    column_name VARCHAR(100) NOT NULL,
    table_name VARCHAR(100) NOT NULL,
    data_type VARCHAR(50), -- string, number, date, geography
    hierarchy JSONB, -- 层次结构
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(name)
);

-- 业务关系表
CREATE TABLE relationships (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(100) NOT NULL,
    source_table VARCHAR(100) NOT NULL,
    source_column VARCHAR(100) NOT NULL,
    target_table VARCHAR(100) NOT NULL,
    target_column VARCHAR(100) NOT NULL,
    relationship_type VARCHAR(50), -- one-to-many, many-to-many
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 看板表
CREATE TABLE boards (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    description TEXT,
    owner_id UUID REFERENCES users(id),
    layout JSONB, -- 布局配置
    cards JSONB, -- 卡片配置
    refresh_schedule VARCHAR(50), -- 刷新计划
    is_public BOOLEAN DEFAULT false,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- 查询历史表
CREATE TABLE query_history (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    question TEXT NOT NULL,
    sql_query TEXT,
    result_schema JSONB,
    execution_time_ms INTEGER,
    ai_agent_logs JSONB, -- AI Agent 决策日志
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- AI 技能表
CREATE TABLE ai_skills (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(100) NOT NULL,
    description TEXT,
    triggers TEXT[], -- 触发关键词
    instructions TEXT NOT NULL, -- 执行指令
    context_tables TEXT[],
    output_schema JSONB,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(name)
);

-- 索引
CREATE INDEX idx_metrics_name ON metrics(name);
CREATE INDEX idx_dimensions_name ON dimensions(name);
CREATE INDEX idx_boards_owner ON boards(owner_id);
CREATE INDEX idx_query_history_user ON query_history(user_id);
CREATE INDEX idx_query_history_created ON query_history(created_at);
```

---

## 🔧 核心模块实现

// DAG 有向无环图
type DAG struct {
    mu      sync.RWMutex
    nodes   map[string]*Cell
    edges   map[string][]string // node -> children
    reverse map[string][]string // node -> parents
}

// NewDAG 创建新的依赖图
func NewDAG() *DAG {
    return &DAG{
        nodes:   make(map[string]*Cell),
        edges:   make(map[string][]string),
        reverse: make(map[string][]string),
    }
}

// AddCell 添加单元格
func (d *DAG) AddCell(cell *Cell) error {
    d.mu.Lock()
    defer d.mu.Unlock()
    
    // 检查循环依赖
    if err := d.wouldCreateCycle(cell.ID, cell.Deps); err != nil {
        return err
    }
    
    d.nodes[cell.ID] = cell
    
    // 更新边
    for _, dep := range cell.Deps {
        d.edges[dep] = append(d.edges[dep], cell.ID)
        d.reverse[cell.ID] = append(d.reverse[cell.ID], dep)
    }
    
    return nil
}

// GetExecutionOrder 获取执行顺序 (拓扑排序)
func (d *DAG) GetExecutionOrder() ([]string, error) {
    d.mu.RLock()
    defer d.mu.RUnlock()
    
    visited := make(map[string]bool)
    order := []string{}
    
    var visit func(id string) error
    visit = func(id string) error {
        if visited[id] {
            return nil
        }
        
        // 先访问所有依赖
        for _, parent := range d.reverse[id] {
            if err := visit(parent); err != nil {
                return err
            }
        }
        
        visited[id] = true
        order = append(order, id)
        return nil
    }
    
    // 遍历所有节点
    for id := range d.nodes {
        if !visited[id] {
            if err := visit(id); err != nil {
                return nil, err
            }
        }
    }
    
    return order, nil
}

// GetDependents 获取依赖某个单元格的所有单元格
func (d *DAG) GetDependents(cellID string) []string {
    d.mu.RLock()
    defer d.mu.RUnlock()
    
    dependents := []string{}
    queue := []string{cellID}
    visited := make(map[string]bool)
    
    for len(queue) > 0 {
        current := queue[0]
        queue = queue[1:]
        
        for _, child := range d.edges[current] {
            if !visited[child] {
                visited[child] = true
                dependents = append(dependents, child)
                queue = append(queue, child)
            }
        }
    }
    
    return dependents
}
```

#### 1.2 响应式执行引擎
---

## 🔧 核心模块实现（待扩展）
    if err != nil {
        return err
    }
    
    // 更新单元格输出
    e.mu.Lock()
    cell.Output = output
    e.mu.Unlock()
    
    // 通知监听器
    e.notifyListeners(cellID, output)
    
    // 触发依赖单元格重新计算
    go e.recomputeDependents(ctx, cellID)
    
    return nil
}

// recomputeDependents 重新计算依赖的单元格
func (e *Engine) recomputeDependents(ctx context.Context, cellID string) {
    dependents := e.dag.GetDependents(cellID)
    
    for _, depID := range dependents {
        select {
        case <-ctx.Done():
            return
        default:
            if err := e.ExecuteCell(ctx, depID); err != nil {
                // 记录错误，继续执行其他单元格
                fmt.Printf("Error recomputing %s: %v\n", depID, err)
            }
        }
    }
}

// OnChange 注册单元格变更监听器
func (e *Engine) OnChange(cellID string, callback func(string, interface{})) {
    e.mu.Lock()
    defer e.mu.Unlock()
    
    e.listeners[cellID] = append(e.listeners[cellID], callback)
}

// notifyListeners 通知监听器
func (e *Engine) notifyListeners(cellID string, output interface{}) {
    e.mu.RLock()
    callbacks := e.listeners[cellID]
    e.mu.RUnlock()
    
    for _, callback := range callbacks {
        go callback(cellID, output)
    }
}
```

---

### 2. 语义层实现

#### 2.1 语义层核心

```go
// backend/internal/service/semantic/layer.go

package semantic

import (
    "context"
    "fmt"
)

// SemanticLayer 语义层
type SemanticLayer struct {
    metrics      *MetricStore
    dimensions   *DimensionStore
    relationships *RelationshipStore
    rules        *BusinessRuleStore
}

// NewSemanticLayer 创建语义层
func NewSemanticLayer() *SemanticLayer {
    return &SemanticLayer{
        metrics:      NewMetricStore(),
        dimensions:   NewDimensionStore(),
        relationships: NewRelationshipStore(),
        rules:        NewBusinessRuleStore(),
    }
}

// ResolveQuery 解析自然语言查询
func (sl *SemanticLayer) ResolveQuery(ctx context.Context, question string) (*ResolvedQuery, error) {
    // Step 1: 使用 AI 解析意图
    intent, err := sl.parseIntent(ctx, question)
    if err != nil {
        return nil, err
    }
    
    // Step 2: 映射指标和维度
    resolved := &ResolvedQuery{
        Intent: intent,
    }
    
    // 映射指标
    for _, metricName := range intent.Metrics {
        metric, err := sl.metrics.GetByName(metricName)
        if err != nil {
            return nil, fmt.Errorf("metric %s not found", metricName)
        }
        resolved.Metrics = append(resolved.Metrics, metric)
    }
    
    // 映射维度
    for _, dimName := range intent.Dimensions {
        dim, err := sl.dimensions.GetByName(dimName)
        if err != nil {
            return nil, fmt.Errorf("dimension %s not found", dimName)
        }
        resolved.Dimensions = append(resolved.Dimensions, dim)
    }
    
    // Step 3: 应用业务规则
    for _, rule := range sl.rules.GetRelevantRules(resolved) {
        resolved = rule.Apply(resolved)
    }
    
    return resolved, nil
}

// GenerateSQL 生成 SQL
func (sl *SemanticLayer) GenerateSQL(resolved *ResolvedQuery) (string, error) {
    builder := NewSQLBuilder()
    
    // SELECT 子句
    for _, dim := range resolved.Dimensions {
        builder.Select(dim.ColumnName)
    }
    for _, metric := range resolved.Metrics {
        builder.Select(fmt.Sprintf("%s as %s", metric.Formula, metric.Name))
    }
    
    // FROM 子句
    tables := sl.collectTables(resolved)
    builder.From(tables[0])
    
    // JOIN 子句
    for i := 1; i < len(tables); i++ {
        relationship := sl.relationships.FindRelationship(tables[0], tables[i])
        if relationship != nil {
            builder.Join(relationship)
        }
    }
    
    // WHERE 子句
    if resolved.Filters != nil {
        builder.Where(resolved.Filters)
    }
    
    // GROUP BY 子句
    if len(resolved.Dimensions) > 0 {
        builder.GroupBy(resolved.Dimensions)
    }
    
    return builder.Build()
}
```

#### 2.2 指标管理

```go
// backend/internal/service/semantic/metrics.go

package semantic

type Metric struct {
    ID          string   `json:"id"`
    Name        string   `json:"name"`
    Description string   `json:"description"`
    Formula     string   `json:"formula"` // SQL 表达式
    Tables      []string `json:"tables"`
    Dimensions  []string `json:"dimensions"`
    Tags        []string `json:"tags"`
    Owner       string   `json:"owner"`
    Format      string   `json:"format"` // number, percentage, currency
}

// MetricStore 指标存储
type MetricStore struct {
    cache map[string]*Metric
}

// GetByName 按名称获取指标
func (ms *MetricStore) GetByName(name string) (*Metric, error) {
    if metric, exists := ms.cache[name]; exists {
        return metric, nil
    }
    return nil, fmt.Errorf("metric %s not found", name)
}

// CreateMetric 创建指标
func (ms *MetricStore) CreateMetric(m *Metric) error {
    // 验证指标名称唯一性
    if _, exists := ms.cache[m.Name]; exists {
        return fmt.Errorf("metric %s already exists", m.Name)
    }
    
    // 验证公式语法
    if err := ms.validateFormula(m.Formula); err != nil {
        return err
    }
    
    ms.cache[m.Name] = m
    return nil
}

// validateFormula 验证指标公式
func (ms *MetricStore) validateFormula(formula string) error {
    // TODO: 实现 SQL 语法验证
    return nil
}
```

---

### 3. AI Agent 编排

#### 3.1 Agent 编排器

```go
// backend/internal/service/agent/orchestrator.go

package agent

import (
    "context"
    "fmt"
)

// AgentOrchestrator Agent 编排器
type AgentOrchestrator struct {
    queryAgent   *QueryAgent
    semanticAgent *SemanticAgent
    insightAgent *InsightAgent
    qualityAgent *QualityAgent
}

// NewAgentOrchestrator 创建编排器
func NewAgentOrchestrator() *AgentOrchestrator {
    return &AgentOrchestrator{
        queryAgent:    NewQueryAgent(),
        semanticAgent: NewSemanticAgent(),
        insightAgent:  NewInsightAgent(),
        qualityAgent:  NewQualityAgent(),
    }
}

// ProcessQuery 处理查询请求
func (ao *AgentOrchestrator) ProcessQuery(ctx context.Context, question string) (*QueryResult, error) {
    logs := make([]AgentLog, 0)
    
    // Step 1: Query Agent 解析意图
    intent, log, err := ao.queryAgent.ParseIntent(ctx, question)
    logs = append(logs, log)
    if err != nil {
        return nil, err
    }
    
    // Step 2: Semantic Agent 映射语义
    resolved, log, err := ao.semanticAgent.Resolve(ctx, intent)
    logs = append(logs, log)
    if err != nil {
        return nil, err
    }
    
    // Step 3: Quality Agent 验证和优化
    sql, log, err := ao.qualityAgent.OptimizeSQL(ctx, resolved)
    logs = append(logs, log)
    if err != nil {
        return nil, err
    }
    
    // Step 4: 执行 SQL (由外部执行)
    // ...
    
    // Step 5: Insight Agent 生成洞察
    insight, log, err := ao.insightAgent.GenerateInsight(ctx, question, result)
    logs = append(logs, log)
    if err != nil {
        return nil, err
    }
    
    return &QueryResult{
        SQL:      sql,
        Data:     result,
        Insight:  insight,
        Logs:     logs, // 完整的 AI 决策日志
    }, nil
}

// AgentLog AI Agent 决策日志
type AgentLog struct {
    Agent     string                 `json:"agent"`
    Action    string                 `json:"action"`
    Input     map[string]interface{} `json:"input"`
    Output    map[string]interface{} `json:"output"`
    Timestamp int64                  `json:"timestamp"`
}
```

#### 3.2 Query Agent

```go
// backend/internal/service/agent/query.go

package agent

import (
    "context"
    "encoding/json"
)

// QueryAgent 查询代理
type QueryAgent struct {
    model LLM
}

// Intent 查询意图
type Intent struct {
    Type       string   `json:"type"` // query, compare, trend, breakdown
    Metrics    []string `json:"metrics"`
    Dimensions []string `json:"dimensions"`
    Filters    []Filter `json:"filters"`
    TimeRange  *TimeRange `json:"time_range,omitempty"`
}

// ParseIntent 解析查询意图
func (qa *QueryAgent) ParseIntent(ctx context.Context, question string) (*Intent, AgentLog, error) {
    prompt := fmt.Sprintf(`
解析用户的自然语言查询，提取意图。

查询问题：%s

请分析并返回以下信息:
1. 查询类型：query(简单查询), compare(对比), trend(趋势), breakdown(下钻)
2. 涉及的指标 (metrics)
3. 涉及的维度 (dimensions)
4. 过滤条件 (filters)
5. 时间范围 (time_range)

返回 JSON 格式。
`, question)
    
    response, err := qa.model.Generate(ctx, prompt)
    if err != nil {
        return nil, AgentLog{}, err
    }
    
    var intent Intent
    if err := json.Unmarshal([]byte(response), &intent); err != nil {
        return nil, AgentLog{}, err
    }
    
    log := AgentLog{
        Agent:  "QueryAgent",
        Action: "ParseIntent",
        Input:  map[string]interface{}{"question": question},
        Output: map[string]interface{}{"intent": intent},
    }
    
    return &intent, log, nil
}
```

---

## 🎨 前端组件设计（待扩展）

  const [isRunning, setIsRunning] = useState(false)
  const [showAI, setShowAI] = useState(false)
  
  const handleRun = async () => {
    setIsRunning(true)
    try {
      await onRun()
    } finally {
      setIsRunning(false)
    }
  }
  
  const renderOutput = () => {
    switch (cell.type) {
      case 'sql':
      case 'python':
        if (Array.isArray(cell.output)) {
          return <DataTable data={cell.output} />
        }
        return <pre>{JSON.stringify(cell.output, null, 2)}</pre>
        
      case 'ai':
        return (
          <AIInsightCard
            insight={cell.output.insight}
            confidence={cell.output.confidence}
            reasoning={cell.output.reasoning}
          />
        )
        
      default:
        return null
    }
  }
  
  return (
    <div className="cell border rounded-lg p-4 my-2">
      <CellHeader
        type={cell.type}
        index={cell.index}
        deps={cell.deps}
        onRun={handleRun}
        onDelete={onDelete}
        onAI={() => setShowAI(!showAI)}
        isRunning={isRunning}
      />
      
      <div className="cell-input mt-2">
        {cell.type === 'sql' && (
          <SQLEditor
            value={cell.code}
            onChange={(code) => onUpdateCode(cell.id, code)}
          />
        )}
        
        {(cell.type === 'python' || cell.type === 'ai') && (
          <CodeEditor
            language="python"
            value={cell.code}
            onChange={(code) => onUpdateCode(cell.id, code)}
          />
        )}
        
        {cell.type === 'markdown' && (
          <MarkdownEditor
            value={cell.code}
            onChange={(code) => onUpdateCode(cell.id, code)}
          />
        )}
      </div>
      
      {showAI && cell.type !== 'markdown' && (
        <AIAssistant
          code={cell.code}
          context={cell.deps}
          onApply={(suggestion) => onUpdateCode(cell.id, suggestion)}
        />
      )}
      
      {cell.output && (
        <div className="cell-output mt-4 p-4 bg-gray-50 rounded">
          {renderOutput()}
        </div>
      )}
    </div>
  )
}
```

---

## 🚀 部署架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                  Ingress Controller                   │   │
│  └──────────────────────────────────────────────────────┘   │
│                           ↓                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Frontend    │  │   Backend    │  │   Compute    │      │
│  │  Deployment  │  │  Deployment  │  │   Worker     │      │
│  │  (Next.js)   │  │   (Go)       │  │  (DuckDB)    │      │
│  │  replicas: 2 │  │  replicas: 3 │  │  replicas: 2 │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                           ↓                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  PostgreSQL  │  │    Redis     │  │  Git Server  │      │
│  │  StatefulSet │  │  StatefulSet │  │  (可选)      │      │
│  │  pvc: 100Gi  │  │  pvc: 10Gi   │  │              │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 性能优化策略

### 1. 查询优化

```yaml
缓存策略:
  - 查询结果缓存：Redis, TTL=5min
  - 指标预计算：定时任务，每小时更新
  - 物化视图：高频查询使用物化视图

SQL 优化:
  - 查询重写：自动优化低效 SQL
  - 索引推荐：分析查询模式，推荐索引
  - 分区策略：大表自动分区

并发控制:
  - 查询队列：限制并发查询数
  - 超时控制：查询超时自动终止
  - 资源配额：按用户分配资源
```

### 2. 前端优化

```yaml
渲染优化:
  - 虚拟滚动：大数据列表
  - 懒加载：组件按需加载
  - Web Workers: 复杂计算

数据优化:
  - 增量更新：只传输变更数据
  - 压缩传输：Gzip/Brotli
  - 本地缓存：IndexedDB

用户体验:
  - 骨架屏：加载占位
  - 乐观更新：先更新后同步
  - 错误边界：局部错误不影响全局
```

---

## 🔒 安全设计

### 1. 访问控制

```yaml
认证:
  - JWT Token 认证
  - OAuth2 集成 (可选)
  - 会话管理

授权:
  - RBAC 模型
  - 行级权限：按用户过滤数据
  - 列级脱敏：敏感字段加密

审计:
  - 操作日志：所有写操作记录
  - 查询审计：记录所有查询
  - 异常检测：可疑行为告警
```

### 2. 数据安全

```yaml
传输加密:
  - HTTPS/TLS
  - 内部服务 mTLS

存储加密:
  - 数据库字段加密
  - 文件存储加密

数据脱敏:
  - 自动识别敏感字段
  - 动态脱敏策略
  - 审计日志完整记录
```

---

## 📈 监控与告警

```yaml
监控指标:
  - 应用性能：QPS, 延迟，错误率
  - 资源使用：CPU, 内存，磁盘
  - 业务指标：活跃用户，查询量，AI 使用率

日志收集:
  - 结构化日志
  - 集中式日志存储
  - 日志分析告警

分布式追踪:
  - OpenTelemetry
  - 请求链路追踪
  - 性能瓶颈分析

告警策略:
  - 错误率 > 5%: P1 告警
  - 延迟 P99 > 2s: P2 告警
  - 资源使用 > 80%: P3 告警
```

---

**BizLens v3.0 技术架构设计完成！**
